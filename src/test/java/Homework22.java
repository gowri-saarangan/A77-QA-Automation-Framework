
import koelapp.BasePage;
import koelapp.LoginPageapp;
import koelapp.playlistpage;
import org.openqa.selenium.By;
import org.testng.Assert;
import org.testng.annotations.Test;
import org.openqa.selenium.support.locators.RelativeLocator;

public class Homework22 extends BaseTest{
 /*   @Test
    public void renamingtheplaylist() throws InterruptedException {
        LoginPageapp LoginPageapp = new LoginPageapp(driver);
        playlistpage playlistpage = new playlistpage(driver);
        BasePage basePage = new BasePage(driver);

        LoginPageapp.setEmailfield("gowri.saarangan@testpro.io");
        LoginPageapp.setPasswordfield("iVRbJcck");
        LoginPageapp.setSubmitbtn();

        Assert.assertTrue(playlistpage.avatardisplayed());

        String listname1 = randomnamegenerator();
        System.out.println(listname1);
        playlistpage.createrenewplaylist(listname1);

        String listname2 = randomnamegenerator();
        System.out.println(listname2);
        playlistpage.playlistrename(listname2,listname1);

      // Assert.assertTrue(basePage.findelements(playlist).isEmpty());
       // playlistpage.listisempty(listname1);
    }*/
}
